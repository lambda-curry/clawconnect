# dev
.yarn/
!.yarn/releases
.vscode/*
!.vscode/launch.json
!.vscode/*.code-snippets
.idea/workspace.xml
.idea/usage.statistics.xml
.idea/shelf

# deps
node_modules/
dist/

# runtime state
.job-store/

# env
.env
.env.production

# logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# misc
.DS_Store
*.zip
*.tgz

# AI agent worktrees
.claude/worktrees/

# Heap snapshots (--heapsnapshot-signal writes these into the process CWD).
# A snapshot is a dump of live memory — tokens, user content, request bodies —
# so it must never be committed, least of all from a public repository.
*.heapsnapshot
